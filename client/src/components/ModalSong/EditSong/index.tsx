import React from "react";

const EditSong = ({
  songId,
  open,
  closeModal,
}: {
  songId: string;
  open: boolean;
  closeModal: () => void;
}) => {
  return <div>EditSong</div>;
};

export default EditSong;
